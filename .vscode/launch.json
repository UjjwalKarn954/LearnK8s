{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/Push_docker_image/package.json",
                "localRoot": "${workspaceFolder}/Push_docker_image"
            }
        }
    ]
}